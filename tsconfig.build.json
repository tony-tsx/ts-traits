{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",                               /* Specify the root folder within your source files. */
  },
  "exclude": ["**/*.spec.ts", "jest.config.ts", "dist"],
}